name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: yarn install --frozen-lockfile
      
    - name: Build marketing dependencies
      run: |
        echo "🔧 Building only marketing app dependencies..."
        yarn workspace @plinto/ui build || echo "UI build failed, continuing..."
        yarn workspace @plinto/react build || echo "React build failed, continuing..."
        yarn workspace @plinto/marketing build || echo "Marketing build failed, continuing..."
      continue-on-error: true
      
    - name: Trigger Vercel Deployment
      if: ${{ secrets.VERCEL_DEPLOY_HOOK != '' }}
      run: |
        echo "🚀 Triggering Vercel deployment..."
        
        if [ -z "$VERCEL_DEPLOY_HOOK" ]; then
          echo "⚠️ VERCEL_DEPLOY_HOOK not configured, skipping deployment"
          exit 0
        fi
        
        echo "Deploy hook URL configured (masked)"
        
        RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "$VERCEL_DEPLOY_HOOK" \
          -H "Content-Type: application/json" \
          -d "{\"ref\":\"$GITHUB_SHA\",\"message\":\"GitHub Actions deploy from commit $GITHUB_SHA\",\"target\":\"production\"}")
        
        HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
        BODY=$(echo "$RESPONSE" | head -n -1)
        
        echo "📡 Response received"
        echo "📊 HTTP status: $HTTP_CODE"
        
        if [ "$HTTP_CODE" -eq 200 ] || [ "$HTTP_CODE" -eq 201 ]; then
          echo "✅ Deploy hook triggered successfully!"
        else
          echo "❌ Deploy hook failed with status $HTTP_CODE"
          echo "Response: $BODY"
          exit 1
        fi
      env:
        VERCEL_DEPLOY_HOOK: ${{ secrets.VERCEL_DEPLOY_HOOK }}
      continue-on-error: true

  notify:
    needs: deploy
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: Deployment Status
      run: |
        if [ "${{ needs.deploy.result }}" == "success" ]; then
          echo "✅ Deployment workflow completed"
        else
          echo "⚠️ Deployment workflow completed with warnings"
        fi
      continue-on-error: true