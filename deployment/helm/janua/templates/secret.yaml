apiVersion: v1
kind: Secret
metadata:
  name: {{ include "janua.fullname" . }}-secret
  labels:
    {{- include "janua.labels" . | nindent 4 }}
type: Opaque
data:
  database-url: {{ include "janua.databaseUrl" . | b64enc | quote }}
  redis-url: {{ include "janua.redisUrl" . | b64enc | quote }}
  jwt-secret-key: {{ .Values.api.env.JWT_SECRET_KEY | b64enc | quote }}
  {{- if .Values.backup.enabled }}
  backup-access-key: {{ .Values.backup.storage.accessKey | b64enc | quote }}
  backup-secret-key: {{ .Values.backup.storage.secretKey | b64enc | quote }}
  {{- end }}