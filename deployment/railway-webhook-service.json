{
  "name": "janua-webhook-service",
  "source": "apps/webhook-service",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  },
  "variables": {
    "PORT": "8003",
    "JANUA_ENV": "production",
    "JWT_AUDIENCE": "janua.dev",
    "JWT_ISSUER": "https://janua.dev"
  },
  "domains": [
    {
      "domain": "webhooks.janua.dev"
    }
  ],
  "networking": {
    "serviceName": "janua-webhook-service",
    "internalPort": 8003
  }
}